---
import VideoWrapper from '../../components/video-wrapper.astro';
import {indexPaths} from '../_paths';

export const getStaticPaths = indexPaths();
// import {getLocale} from 'astro-i18n-aut';

import {getLocaleFromUrl, useTranslations} from '../../i18n/i18n.ts';
import DefaultLayout from '../../layouts/DefaultLayout.astro';
import CtaBubble from '../../components/ui/cta-bubble.astro';

// const locale = getLocale(Astro.url) as 'en' | 'de';
const locale = getLocaleFromUrl(Astro.url);
//const locale = 'de';
const t = useTranslations(locale);
const data = Astro.locals;
---

<script>
  // import globals from 'globals';
</script>

<DefaultLayout>
  <VideoWrapper />

  <Fragment>
    <div
      class="text-text_difference g flex flex-wrap items-center justify-center gap-2 md:flex-nowrap"
    >
      <h1
        class="text-step-5 sm:text-step-6 md:text-step-8 mt-4 w-fit leading-none tracking-tight md:border-r-2"
        id="buchungskalender"
      >
        {t('general.skurrilum')}
      </h1>
      <span
        class="text-step-0 text-center"
        set:text={t('general.descriptionShort')}
      />
    </div>
    <p
      class=" my-6  py-4 text-justify text-lg font-normal"
    >
      {t('general.description')}
    </p>
    <h2>{t('bookingCalender')}</h2>
    <!-- <script
      src="https://cdn.quinbook.com/shop.js"
      data-shopid="01161aaa0b6d1345dd8fe4e481144d84"></script> -->

    <h2>{t('seo.title1')}</h2>
    <p>
      {t('seo.description1')}
    </p>
    <h2>{t('seo.title2')}</h2>
    <p>
      {t('seo.description2')}
    </p>
    <h2>{t('seo.title3')}</h2>
    <p>
      {t('seo.description3')}
    </p>
  </Fragment>
</DefaultLayout>
