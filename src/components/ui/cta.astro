---
interface Props {
  id?: string;
  label: string;
  class?: string;
}

const {label = '', class: className = '', id} = Astro.props;
---

<a
  id={id}
  href="#"
  class:list={[
    '',
    className,
  ]}>{label}</a
>
